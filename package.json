{
  "name": "aqa_1-v02",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test:electron": "npx cypress run --config-file configs/electron.config.js",
  "test:firefox": "cypress run --config-file configs/firefox.config.js",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.4",
    "allure-cypress": "^3.2.1",
    "cypress": "^14.2.1",
    "dotenv": "^16.4.7",
    "esbuild": "^0.25.1"
  }
}
